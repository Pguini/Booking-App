# Backend setup
This backend is purely for demonstration. <p/>
Run the following code to install the dependencies <p/>
pip install -r requirements.txt <p/>

The app.py file contains the main backend logic. <p/>
<b>The server is assumed to run locally.</b> <p/>
